# 📋 План: Рафиниране на Мерни Единици и Actuator State (Task 10)

В продължение на Task 9, целта на този план е да изчистим логическите конфликти при конвертирането на единици (Сензори) и отчитането на статус (Актуатори).

## 🎯 Цели
1.  **Сензори:** `lastReading.unit` да бъде "Източник на истината". Ако стратегията промени единицата (напр. mm -> L), това трябва да е водещото в UI, а не `baseUnit`.
2.  **Актуатори:** `lastReading.value` винаги да бъде `0` или `1` (Binary State). Всички допълнителни изчисления от стратегии (напр. дебит 0.005 L/s) да отиват в `details`, за да не чупят логиката за ON/OFF статус.

---

## 🏗️ Фаза 1: Сензори - Динамични Единици
**Задача:** Уверяване, че стратегията диктува единицата.

- [x] **Преглед на `ConversionService.ts`:**
    - Проверка дали всички стратегии връщат правилния `unit` в резултата.
- [x] **Преглед на `SensorProcessor.ts`:**
    - Уверяване, че при `processRawToBasic` и `processBasicToDisplay`, динамичната единица от стратегията (`smartResult.unit`) презаписва `baseUnit` или `template unit`.
    - **Важно:** `lastReading.unit` в базата трябва да отразява това.
- [x] **UI Priority Fix (`ExpandedControllerView`, `PinnedSensorsGrid`, `DeviceTestDialog`):**
    - Обърнахме приоритета: `lastReading.unit` > `displayUnit`. Така UI-ът показва реалната единица от измерването (напр. Liters), ако има такава, вместо да насилва `displayUnit` (mm).

---

## 🔌 Фаза 2: Актуатори - Binary State Enforcer
**Задача:** "Actuator Value is Sacred" (Винаги 0 или 1).

- [x] **Модификация на `SensorProcessor.ts`:**
    - В метод `processRawToBasic`:
        - Добавяне на проверка: `if (device.type === 'ACTUATOR')`.
        - Ако е Актуатор:
            - Запази оригиналния резултат от стратегията (напр. дебит) в `details`.
            - Насилствено установи `value`:
                - `1` (ако raw > 0 или стратегията връща > 0)
                - `0` (ако raw === 0)
            - Насилствено установи `unit` = `'boolean'` (или вземи `baseUnit`, който вече е `boolean`).
- [ ] **Тестване на Логиката:**
    - Симулация на четене от Dosing Pump.
    - Уверяване, че `value` е 1, а не 0.005.

---

## 🧪 Фаза 3: Валидация
- [ ] **Тест Сензор (Volume):**
    - Настройка на сензор с `tank_volume` стратегия.
    - Проверка в DB: `lastReading` трябва да има `unit: 'L'` (или както е настроено), а не `mm`.
- [ ] **Тест Актуатор (Pump):**
    - Пускане на помпа.
    - Проверка в DB: `lastReading.value` трябва да е `1`.
    - UI трябва да свети в Зелено (ON).
