// === SYSTEM COMMANDS (DEFAULT/STUB IMPLEMENTATION) ===
// Fallback for unknown architectures

// === MEMORY ===
int freeMemory() {
  return 0; // Unknown
}

// === RESET ===
void resetDevice() {
  // No-op or soft reset attempt
}
