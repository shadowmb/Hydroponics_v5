# Backend Environment Variables

# Server Configuration
PORT=5000
NODE_ENV=development

# Frontend Configuration
# Port where frontend runs (used for CORS validation)
FRONTEND_PORT=3000

# Database Configuration
MONGO_URI=mongodb://localhost:27017/hydroponics

# CORS Configuration
# CORS is now handled dynamically - accepts requests from any hostname on FRONTEND_PORT
# This allows the system to work on any IP address without configuration changes
